**Before testing these scripts!**

You need to make sure that the target machine is not cut off from the net.
In Kali terminal: echo 1 >> /proc/sys/net/ipv4/ip_forward

Then first Start the Arpspoofer then the sniffer.

Sniffer only work atm with HTTP NOT HTTPS!

